import {useLocation, Navigate, Outlet} from 'react'

const RequireAuth = () => {
    const location = useLocation();
    return (
        <div>
        
        </div>
    )
}

export default RequireAuth
